{
  "scripts": {
    "test": "jest --watch",
    "coverage": "jest --coverage"
  },
  "devDependencies": {
    "coverage": "^0.4.1",
    "jest": "^28.1.2",
    "jest-environment-jsdom": "^28.1.2"
  }
}
